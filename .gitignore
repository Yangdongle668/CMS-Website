node_modules/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
.idea/
.vscode/
uploads/*
!uploads/.gitkeep
coverage/
dist/
backups/
# Persistent app data outside Docker. Holds secrets.json (the generated
# JWT/cookie signing keys) and the emergency inquiry store — never commit it.
data/
